import TopBar from "../topBar/topBar.component";
import MenuTop from "../menu/menu.component";

const Header = () => (
  <div className="header">
    <TopBar />
    <MenuTop />
  </div>
);

export default Header;
